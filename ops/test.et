indices[slice,coord] = RANDOM(0, DIMS(dest)[coord], INT)
updates[batch,slice,elem] = RANDOM(0, 10, FLOAT)
output[batch,dest,elem] = 0.0 
output[batch,indices[slice,:],elem] = updates[batch,slice,elem]

tf.scatter_nd(indices=indices, updates=updates, shape=TENSOR(DIMS(batch,dest,elem)))

output

RANK(batch) = 1
RANK(slice) IN [0,4]
RANK(elem) IN [1,4]
RANK(dest) IN [1,3]
DIMS(elem) IN [1,15]
DIMS(dest) IN [1,15]
RANK(coord) = 1
DIMS(coord) = RANK(dest)
DIMS(slice) IN [0,15]
