params[batch,elem,other] = RANDOM(0, DIMS(elem)[coord], FLOAT)
indices[batch,slice,coord] = RANDOM(0, 10, INT)
result[batch,slice,other] = params[batch,indices[batch,slice,:],other]

tf.gather_nd(params, indices, batch_dims=RANK(batch))

result

RANK(batch,slice,other) < 8
RANK(batch,elem,other) < 8
RANK(coord) == 1
DIMS(coord) == RANK(elem) >= 1
DIMS(batch) < 5
DIMS(slice) < 20
0 < DIMS(elem) < 20
0 < DIMS(other) < 20


