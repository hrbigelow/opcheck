{
  "program": [
    "params[batch,elem,other] = RANDOM(0, DIMS(elem)[coord], FLOAT)",
    "indices[batch,slice,coord] = RANDOM(0, 10, INT)",
    "result[batch,slice,other] = params[batch,indices[batch,slice,:],other]"
  ],
  "constraints": [
    "RANK(batch) + RANK(slice) + RANK(other) < 8",
    "RANK(batch) + RANK(elem) + RANK(other) < 8",
    "RANK(elem) > 0",
    "RANK(coord) == 1",
    "DIMS(coord)[0] == RANK(elem)"
  ],
  "tfcall": {
    "func": "tf.gather_nd",
    "args": {
      "params": "params",
      "indices": "indices",
      "batch_dims": "RANK(batch)"
    },
    "return-value": "result"
  }
}

