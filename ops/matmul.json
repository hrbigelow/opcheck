{
  "program": [
    "mat1[batch,row,inner] = RANDOM(0, 10)",
    "mat2[batch,inner,col] = RANDOM(0, 10)",
    "result[batch,row,col] = mat1[batch,row,inner] * mat2[batch,inner,col]"
  ],
  "constraints": [
    "RANK(batch) < 10",
    "RANK(row) == 1",
    "RANK(inner) == 1",
    "RANK(col) == 1"
  ],
  "tfcall": {
    "func": "tf.linalg.matmul",
    "args": {
      "a": "mat1",
      "b": "mat2"
    },
    "return-value": "result"
  }
}

