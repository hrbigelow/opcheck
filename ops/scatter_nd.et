indices[slice,coord] = RANDINT(0, DIMS(elem)[coord], INT)
updates[slice,elem] = RANDOM(0, 10, FLOAT)
output[dest,elem] = 0.0 
output[indices[slice,:],elem] = updates[slice,elem]

tf.scatter_nd(indices, updates, DIMS(output)[:])

output

RANK(coord) == 1
DIMS(coord)[0] <= RANK(elem)
RANK(slice) < 8
RANK(slice) > 0
RANK(elem) < 8

