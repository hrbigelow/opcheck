{
  "program": [
    "indices[s,c] = RANDINT(0, DIMS(e)[c])",
    "updates[s,f] = RANDOM()",
    "tensor[e,f] = RANDOM()",
    "intensor[e,f] = tensor[e,f]",
    "tensor[indices[s,:],f] += updates[s,f]"
  ],
  "constraints": [
    "RANK(c) == 1",
    "RANK(s) < 8",
    "RANK(s) > 0",
    "RANK(f) > 0",
    "RANK(f) < 8",
    "RANK(e) > 0",
    "RANK(e) < 8",
    "DIMS(c)[0] == RANK(e)"
  ],
  "tfcall": {
    "func": "tf.tensor_scatter_nd_add",
    "args": {
      "tensor": "intensor",
      "indices": "indices",
      "updates": "updates"
    },
    "return-value": "tensor"
  }
}

